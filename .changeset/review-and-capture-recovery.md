---
"waveform-one": patch
---

Prevent Codex reviews from posting against a changed PR head and bind each review
to its reviewed commit. Send every ESP capture-control reply through checked USB
writes, abort captures whose header cannot be sent, and reset queued audio and
producer state safely before starting another recording.
