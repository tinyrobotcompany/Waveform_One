---
"waveform-one": patch
---

Report the full ESP firmware identity for update health checks, verify it against the packaged image, and support a persistent release-sequence offset for CI migrations. Add regression coverage for discovery URLs, empty HTTP checks, and timeout cleanup/retry.
