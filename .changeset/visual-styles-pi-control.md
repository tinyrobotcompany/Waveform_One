---
"waveform-one": minor
---
Add mirrored spectrum and scrolling waterfall alongside the original classic
bars. Introduce a versioned USB control protocol and Rust Pi controller with
acknowledged mode switching. Add renderer/protocol tests and ARM64 controller
builds and release downloads. The ESP keeps processing audio independently;
the touchscreen UI, persistent Pi daemon and OTA remain subsequent milestones.
